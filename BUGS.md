# Bug Reports — 2026-09-11

Τρία θέματα που ανέφερε ο Δημήτρης. Για κάθε ένα: τι παρατηρείται, γιατί συμβαίνει
(εντοπισμένο στον κώδικα), και ποια είναι η διόρθωση.

---

## 1. Τριπλή ειδοποίηση "X sessions left"

**Παρατήρηση:** Πελάτης με 2 sessions left έλαβε 3 φορές το ίδιο notification
("You have 2 sessions left..."). Υποψία ότι συμβαίνει σε όλους τους πελάτες όταν
πέφτουν στο κατώφλι των 1-2 sessions, όχι μόνο σε αυτόν.

**Αιτία:** Το notification στέλνεται από ΔΥΟ ανεξάρτητα σημεία —
`ClientApp.jsx` (κάθε φορά που φορτώνει η εφαρμογή του πελάτη) και
`TrainerApp.jsx` (κάθε φορά που ο trainer ανοίγει την κάρτα του πελάτη). Και τα
δύο ελέγχουν πρώτα αν έχει ήδη σταλεί ειδοποίηση για το συγκεκριμένο threshold
(μέσω της στήλης `packages.low_sessions_alert_level`) και μετά γράφουν την τιμή —
αλλά αυτός ο έλεγχος-και-μετά-εγγραφή δεν είναι atomic. Αν δύο φορτώσεις τρέξουν
κοντά χρονικά (π.χ. trainer ανοίγει την κάρτα ενώ ο πελάτης έχει ανοιχτό το app σε
άλλη συσκευή, ή ο πελάτης κάνει pull-to-refresh ενώ ένα realtime event ξαναφορτώνει
τα δεδομένα), και οι δύο διαβάζουν την ίδια «δεν έχει ειδοποιηθεί ακόμα» κατάσταση
πριν προλάβει να «κλειδώσει» η πρώτη εγγραφή στη βάση — και οι δύο στέλνουν push.
Αυτό είναι ένα ήδη γνωστό, εν μέρει διορθωμένο θέμα (βλ. `sql/015_low_sessions_notif_dedup.sql`)
που έμεινε ευάλωτο στο race condition.

**Διόρθωση:** Το PATCH που «κλειδώνει» το alert level μετατράπηκε σε atomic
compare-and-swap — γίνεται conditional UPDATE στη βάση (`low_sessions_alert_level
IS NULL OR > newLeft`) και το notification στέλνεται ΜΟΝΟ αν το UPDATE όντως
πέτυχε (επέστρεψε γραμμή). Έτσι, όσες κλήσεις κι αν τρέξουν ταυτόχρονα, μόνο η
πρώτη θα «κερδίσει» και θα στείλει notification — οι υπόλοιπες θα δουν ότι η
γραμμή δεν ταιριάζει πια και θα σιωπήσουν.

**Αρχεία:** `src/ClientApp.jsx` (loadData), `src/TrainerApp.jsx` (auto-settle effect)

---

## 2. Schedule: η ολοκληρωμένη προπόνηση της ίδιας μέρας δεν φαίνεται

**Παρατήρηση:** Πελάτης έχει κλείσει ραντεβού, έκανε την προπόνηση, βλέπει το
tick/ένδειξη πάνω στη μέρα (π.χ. Δευτέρα) στη λωρίδα ημερών, αλλά όταν πατάει
πάνω στη μέρα δεν βλέπει το συγκεκριμένο ραντεβού που έκανε (π.χ. "Δευτέρα 17:00").

**Αιτία:** Η οθόνη Schedule αποφασίζει τι θα δείξει με βάση `isPastDay`
(`selDay.iso < todayStr`) — αυστηρά "μέρα πριν από σήμερα". Για το ΣΗΜΕΡΑ, ακόμα
κι αν η προπόνηση έχει ήδη ολοκληρωθεί (πέρασε η ώρα + το grace period), η
συνθήκη είναι false, οπότε ο κώδικας πάει στο κλάδο που δείχνει το grid των
διαθέσιμων slots προς κράτηση — όχι στο κλάδο που δείχνει "αυτό είναι το
ραντεβού σου, ολοκληρώθηκε". Και επειδή η ώρα του slot έχει ήδη περάσει, το
slot αυτό φιλτράρεται ΚΑΙ από το grid των διαθέσιμων ωρών (`visibleSlots`
δείχνει μόνο ώρες `>= τώρα`). Αποτέλεσμα: για ΣΗΜΕΡΑ μετά την προπόνηση, ο
πελάτης δεν βλέπει τίποτα σχετικό — ούτε το grid (σωστά κρυμμένο), ούτε την
κάρτα της ολοκληρωμένης προπόνησης (γιατί αυτός ο κλάδος τρέχει μόνο για
γνήσια περασμένες μέρες).

**Διόρθωση:** Ο έλεγχος επεκτάθηκε ώστε να δείχνει την κάρτα της ολοκληρωμένης
προπόνησης και όταν η επιλεγμένη μέρα είναι το ΣΗΜΕΡΑ και υπάρχει session με
`status === "completed"` γι' αυτή την ημερομηνία — όχι μόνο για γνήσια περασμένες
μέρες. Αν σήμερα υπάρχει μόνο κράτηση που ΔΕΝ έχει ολοκληρωθεί ακόμα (π.χ. είναι
αργότερα το απόγευμα), η οθόνη συνεχίζει να δείχνει κανονικά το booking grid.

**Αρχείο:** `src/ClientApp.jsx` (ScheduleScreen)

---

## 3. Λάθος/ελλιπές Session History στο προφίλ + αδυναμία Notes

**Παρατήρηση:** Ως πελάτης, στο προφίλ, η ενότητα "Session History" δεν δείχνει
σωστά όλες τις προπονήσεις που έχουν γίνει. Θέλει να εμφανίζονται όλες εκεί, και
να μπορεί να προσθέσει Notes όποτε θέλει.

**Αιτία:** Υπάρχουν στην πράξη ΔΥΟ τρόποι με τους οποίους μια προπόνηση
«μετράει» ως ολοκληρωμένη σε αυτό το app:
  1. Μια πραγματική γραμμή στον πίνακα `sessions` (καταγράφεται όταν ο trainer
     κάνει "Log session" χειροκίνητα, ή μέσω του remote-workout flow).
  2. Μια self-booked κράτηση στον πίνακα `bookings` (μέσω του booking grid) της
     οποίας η ώρα + το grace period έχουν περάσει — χωρίς ποτέ να δημιουργηθεί
     αντίστοιχη γραμμή στο `sessions`.

Το **πόσα sessions έχουν καταναλωθεί από το πακέτο** υπολογίζεται ήδη σωστά και
από τις δύο πηγές μαζί (`sessionsMath.js` → `completedItems`/`computeCompletedUsed`,
χρησιμοποιείται και στο Stats panel). Όμως το **Session History στο Profile**
(και η προβολή περασμένης μέρας στο Schedule) διαβάζει ΜΟΝΟ τον πίνακα `sessions`
— άρα κάθε προπόνηση που έγινε αποκλειστικά μέσω self-booking (χωρίς ποτέ να
καταγραφεί χειροκίνητα από τον trainer) απλά δεν εμφανίζεται εκεί, παρότι κανονικά
"μέτρησε" και αφαίρεσε session από το πακέτο.

Επιπλέον, το Notes feature (`session_notes` πίνακας) απαιτεί `session_id` — άρα
για μια τέτοια "ορφανή" ημέρα (μόνο booking, καμία γραμμή `sessions`) δεν υπάρχει
καν πού να κολλήσει το σημείωμα.

**Διόρθωση:** Προστέθηκε νέο, privileged (service-key) endpoint
`api/backfill-sessions.js` που, με βάση την ΙΔΙΑ κοινή λογική του
`sessionsMath.js` (ώστε να μη διαφωνεί ποτέ με το πόσα sessions "μέτρησαν"),
εντοπίζει ημέρες που έχουν ήδη ολοκληρωθεί μέσω self-booking αλλά δεν έχουν
ακόμα γραμμή στο `sessions`, και τις δημιουργεί αυτόματα (status: completed).
Καλείται μία φορά σε κάθε φόρτωση του Client app (αφού έχει ήδη γίνει sign-in),
idempotent — ασφαλές να καλείται επανειλημμένα, δεν δημιουργεί ποτέ διπλή γραμμή
για την ίδια ημέρα. Μόλις δημιουργηθεί η γραμμή, εμφανίζεται αυτόματα παντού όπου
ήδη διαβάζεται ο πίνακας `sessions` (Session History στο Profile, περασμένη μέρα
στο Schedule) — και επειδή πλέον έχει πραγματικό `session_id`, το πεδίο Notes
δουλεύει κανονικά χωρίς άλλη αλλαγή στο UI.

Οι πελάτες δεν έχουν δικαίωμα INSERT στο `sessions` (RLS) — γι' αυτό χρειάστηκε
server-side endpoint με service key, ακριβώς το ίδιο pattern με το ήδη υπάρχον
`api/log-remote-workout.js`.

**Αρχεία:** νέο `api/backfill-sessions.js`, `src/ClientApp.jsx` (loadData)

---

## Status
- [x] #1 — Διορθώθηκε (atomic dedup και στα δύο apps)
- [x] #2 — Διορθώθηκε (Schedule δείχνει τη σημερινή ολοκληρωμένη προπόνηση)
- [x] #3 — Διορθώθηκε (backfill endpoint + αυτόματη κλήση στο load)
- [x] #4 — Διορθώθηκε (custom time requests: 1 pending ανά πελάτη, jump-to-day, καθάρισμα stale notifications)
- [x] #5 — Διορθώθηκε (auto-expire πακέτου με sessions ανεξόφλητα)
- [x] #6 — Διορθώθηκε (renewal με εκκρεμή future bookings παραπάνω από σήμερα)
- [x] #7 — Διορθώθηκε (force-log σήμερα μαρκαρόταν "completed" πρόωρα)
- [x] #8 — Διορθώθηκε (renewal δεν προγέμιζε injury/πρόγραμμα/remote από το τρέχον πακέτο)
- [x] #9 — Διορθώθηκε (override "sessions used" αποθηκεύεται πλέον ως μόνιμο adjustment)
- [x] #10 — Διορθώθηκε (Adjust Package δεν έστελνε καμία ειδοποίηση στον πελάτη)

Χρειάζεται manual test στο πραγματικό app (dev server ή Vercel preview) πριν
γίνει merge/deploy — βλ. οδηγίες setup στο README/CONTEXT.md.

---

## 4. Custom Time Requests — ευχρηστία trainer + no-continuity spam

**Παρατήρηση:** Όταν ανοίγει το trainer app και υπάρχει pending custom-time
request, εμφανίζεται popup με το μήνυμα, αλλά ο trainer δεν έχει εύκολο τρόπο να
δει ΣΕ ΠΟΙΑ ΜΕΡΑ αναφέρεται (ώστε να ξέρει αν μπορεί να το αποδεχτεί) — μπορεί
μόνο να κάνει μια κίνηση (approve/reject) "στα τυφλά". Επιπλέον, στο παρελθόν
συνέβη: πελάτης έκανε 2-3 requests για το ίδιο πράγμα (επειδή δεν έβλεπε αν το
πρώτο είχε σταλεί/απαντηθεί), ο trainer απάντησε σε ένα, τα άλλα έμειναν pending
για πάντα χωρίς συνέχεια.

**Αιτία:**
1. Το pending-requests panel στο Schedule του trainer έδειχνε raw ISO ημερομηνία
   (π.χ. `2026-09-15`) χωρίς μέρα εβδομάδας, και δεν υπήρχε τρόπος να δει το
   ημερολόγιο εκείνης της μέρας πριν αποφασίσει.
2. Δεν υπήρχε ΚΑΜΙΑ επικύρωση που να εμποδίζει έναν πελάτη να στείλει πολλαπλά
   pending requests — το local state (`reqSent`) που "κλείδωνε" το κουμπί
   επαναφερόταν σε κάθε αλλαγή μέρας/εβδομάδας ή reload του app, οπότε ένας
   πελάτης μπορούσε να στείλει απεριόριστα requests.
3. Το notification "X requested a custom time..." στο bell του trainer έμενε
   εκεί για πάντα, ακόμα κι αφού το request είχε ήδη απαντηθεί — καμία ένδειξη
   "answered" vs "pending".

**Διόρθωση:**
- **DB constraint (νέο):** `sql/018_slot_requests_single_pending.sql` — unique
  index που εγγυάται ΤΟ ΠΟΛΥ 1 pending request ανά πελάτη, σε επίπεδο βάσης (όχι
  μόνο UI-check, exactly όπως η ίδια λογική στο bug #1 — δεν πρέπει να μπορεί να
  ξεπεραστεί με race condition). Καθαρίζει αυτόματα τυχόν ήδη υπάρχοντα διπλά
  pending rows πριν φτιάξει το index.
- **Client:** Το Schedule screen δείχνει πλέον live το status του (ενός) pending
  request του πελάτη ("⏳ Δευτέρα, 15 Σεπ στις 17:00 — waiting for your trainer"),
  αντί για τη φόρμα request, μέχρι να απαντηθεί. Ανανεώνεται αυτόματα (realtime)
  μόλις ο trainer απαντήσει. Αν παρ' όλα αυτά γίνει race (π.χ. δύο tabs), ο DB
  constraint το μπλοκάρει και εμφανίζεται φιλικό μήνυμα, όχι raw DB error.
- **Trainer:** Κάθε pending request στο panel δείχνει τώρα "Δευ, 15 Σεπ · 17:00 ·
  view day ›" — πατώντας πάνω, το ημερολόγιο από κάτω πηδάει ακριβώς σε εκείνη
  τη μέρα, ώστε να βλέπει τι άλλο είναι ήδη κλεισμένο πριν αποφασίσει. Το
  notification panel (κουδουνάκι) έχει τώρα "Decide →" και για slot_request
  (όπως ήδη είχε για cancel_request), που πάει κατευθείαν στο Schedule tab. Μόλις
  ο trainer απαντήσει (approve/reject), το παλιό "X requested..." notification
  διαγράφεται αυτόματα — δεν μένει "κρεμασμένο" σαν να μην έχει απαντηθεί.

**Αρχεία:** `sql/018_slot_requests_single_pending.sql`, `src/ClientApp.jsx`
(ScheduleScreen), `src/TrainerApp.jsx` (ScheduleScreen, TrainerNotifPanel)

---

## 5. Πακέτο μπορεί να "κλείσει" σιωπηλά με sessions ανεξόφλητα

**Παρατήρηση:** Ζητήθηκε γενική επιθεώρηση της λογικής πακέτων ("να μην χάνονται
sessions").

**Αιτία:** Σε κάθε φόρτωση του trainer app, ΚΑΘΕ πακέτο με περασμένη `end_date`
απενεργοποιούνταν αυτόματα (`is_active:false`) — ανεξάρτητα από πόσα sessions είχαν
μείνει αχρησιμοποίητα. Η `end_date` είναι απλά μια εκτίμηση (weeks×sessions_per_week
από τη δημιουργία), όχι πραγματικό όριο· το πραγματικό όριο είναι το `sessions_total`
που πλήρωσε ο πελάτης. Μηδενική ειδοποίηση σε πελάτη ή trainer όταν συνέβαινε αυτό, και
πουθενά στο UI δεν υπάρχει "Reactivate" — το πακέτο απλά περνάει στο Package History
ως ανενεργό, χωρίς τρόπο να ξαναχρησιμοποιηθεί από το app. Αν ο πελάτης είχε π.χ. 3
sessions ακόμα (καθυστέρησε λόγω ταξιδιού/ασθένειας), αυτά χάνονταν οριστικά τη στιγμή
που ο trainer ξανάνοιγε το app μετά την ημερομηνία λήξης.

**Διόρθωση:** Το auto-deactivate τρέχει τώρα ΜΟΝΟ όταν ΚΑΙ η ημερομηνία έχει περάσει
ΚΑΙ τα sessions έχουν όντως εξαντληθεί (`sessions_used >= sessions_total`). Ένα πακέτο
με sessions ανεξόφλητα παραμένει ενεργό ανεξάρτητα από την `end_date` — δεν χάνεται
ποτέ πια κάτι σιωπηλά. (Δεν άλλαξε τίποτα στο πώς μπλοκάρεται μια νέα κράτηση όταν
όντως εξαντληθούν τα sessions — αυτό γινόταν ήδη σωστά, ανεξάρτητα από το `is_active`.)

**Αρχείο:** `src/TrainerApp.jsx` (loadData — auto-expire sweep)

---

## 6. Renewal με εκκρεμή ραντεβού πέρα από το "σήμερα"

**Παρατήρηση:** Ίδια επιθεώρηση, ίδιο θέμα "να μην χάνονται sessions".

**Αιτία:** Όταν ο trainer κάνει renew ενώ ο πελάτης έχει ήδη μελλοντικές κρατήσεις
(π.χ. σε 5 μέρες) από το ΠΑΛΙΟ πακέτο, το app ήδη προειδοποιεί "these bookings will
remain" — αλλά δεν τις προστάτευε από το να μετρήσουν ΚΑΤΑ του ΝΕΟΥ πακέτου μόλις
ολοκληρωθούν (charge-at-completion κοιτάζει μόνο ημερομηνία >= `start_date` του
ενεργού πακέτου, όχι ποιο πακέτο ήταν ενεργό όταν έγινε η κράτηση). Υπήρχε ήδη
διόρθωση γι' αυτό, αλλά ΜΟΝΟ για κράτηση δωθεν "σήμερα" (push το `start_date` στο
"αύριο") — μια κράτηση 5 μέρες μπροστά δεν προστατευόταν καθόλου.

**Διόρθωση:** Γενικεύτηκε η ίδια λογική: το `start_date` του νέου πακέτου πηδάει
τώρα πάντα ΜΕΤΑ την ΤΕΛΕΥΤΑΙΑ εκκρεμή ημερομηνία (booking ή session), όχι μόνο μετά
το σήμερα. Έτσι καμία ήδη προγραμματισμένη μέρα από το παλιό πακέτο δεν "τρώει"
session από το φρέσκο πακέτο. Το μήνυμα της προειδοποίησης ενημερώθηκε να λέει
ρητά ότι αυτές οι κρατήσεις δεν θα χρεωθούν στο νέο πακέτο.

**Αρχείο:** `src/TrainerApp.jsx` (handleRenew)

---

## 7. Force-log για ώρα αργότερα σήμερα μαρκαρόταν "completed" πρόωρα

**Αιτία:** Στο Schedule του trainer, όταν force-λόγκαρε πελάτη σε ένα slot, ο
υπολογισμός του status συνέκρινε ΜΟΝΟ ημερομηνίες (`selDay.iso>todayISO()`), όχι
πλήρη ημερομηνία+ώρα. Αποτέλεσμα: force-log πελάτη σε slot 21:00 ΣΗΜΕΡΑ, ενώ είναι
ακόμα πρωί, μαρκαρόταν αμέσως ως "completed" — εμφανιζόταν στο Session History του
πελάτη σαν να έχει ήδη γίνει, ώρες πριν όντως συμβεί, ενώ ταυτόχρονα το notification
έλεγε "your trainer **scheduled** a session for you" (αντιφατικό μήνυμα). Δεν
επηρέαζε την πραγματική μέτρηση sessions (αυτή βασίζεται ήδη σε ώρα+grace period,
όχι στο stored status), ήταν καθαρά θέμα εμφάνισης/μηνύματος — αλλά μπερδεύει.

**Διόρθωση:** Η σύγκριση χρησιμοποιεί τώρα πλήρη ημερομηνία+ώρα του slot (ίδιο
pattern με το ήδη σωστό `handleLog`). Το notification μήνυμα αλλάζει ανάλογα
("scheduled" για μελλοντικό, "logged" για ήδη περασμένο).

**Αρχείο:** `src/TrainerApp.jsx` (handleForceLog)

---

## 8. Renewal δεν προγέμιζε τίποτα από το τρέχον πακέτο

**Αιτία:** Το πάνελ "↻ Renew" ξεκινούσε πάντα από κενά defaults (χωρίς injury flag,
χωρίς πρόγραμμα, in-person, 10 sessions/3x/week) — ΠΟΤΕ δεν προγέμιζε από το ΤΡΕΧΟΝ
ενεργό πακέτο του πελάτη, ούτε σε φρέσκο άνοιγμα της σελίδας. Αν ο trainer δεν
θυμόταν να τα ξαναβάλει χειροκίνητα σε ΚΑΘΕ ανανέωση, το νέο πακέτο σιωπηλά έχανε:
το injury flag/σημειώσεις (safety-critical), το ανατεθειμένο πρόγραμμα, το
remote/in-person delivery mode.

**Διόρθωση:** Το κουμπί "↻ Renew" προγεμίζει τώρα όλα αυτά τα πεδία από το τρέχον
ενεργό πακέτο όταν ανοίγει το πάνελ — ο trainer βλέπει "ό,τι ισχύει τώρα" ως αρχικές
τιμές και μπορεί να τις αλλάξει, αντί να ξεκινά από μηδέν κάθε φορά.

**Αρχείο:** `src/TrainerApp.jsx` (Renew button onClick)

---

## 9. "Override sessions used" προς τα κάτω δεν κολλούσε

**Παρατήρηση:** Στο πάνελ "Adjust Package", το πεδίο "Override sessions used" δεν
"κολλούσε" — αν ο trainer έβαζε έναν αριθμό ΜΙΚΡΟΤΕΡΟ από τον πραγματικό αριθμό
sessions/bookings που είχαν όντως ολοκληρωθεί (π.χ. για να χαρίσει ένα session ως
δώρο/αποζημίωση), φαινόταν να δουλεύει αμέσως — αλλά την ΕΠΟΜΕΝΗ φορά που ο trainer
άνοιγε την κάρτα αυτού του πελάτη, ο αυτόματος μηχανισμός "auto-settle" το
ξαναδιόρθωνε σιωπηλά πίσω στην πραγματική τιμή, χωρίς προειδοποίηση. Η αντίθετη
φορά ("αύξησε το used περισσότερο από την πραγματικότητα", π.χ. χρέωση για no-show
που δεν καταγράφηκε αλλού) δούλευε κανονικά — μόνο επειδή δεν συγκρουόταν ποτέ με
τον αυτόματο υπολογισμό.

**Αιτία:** Το override γραφόταν ως μία ΤΕΛΙΚΗ τιμή στο `sessions_used`. Κάθε φόρτωση
όμως ξαναϋπολογίζει το "used" από τα πραγματικά δεδομένα (sessions/bookings) και το
ξαναγράφει — αν η χειροκίνητη τιμή διαφωνούσε με τον πραγματικό υπολογισμό, χανόταν.

**Διόρθωση:** Το override αποθηκεύεται τώρα ως **μόνιμο adjustment** (νέα στήλη
`sessions_used_adjustment`) πάνω στον πραγματικό υπολογισμό, όχι ως τελική τιμή που
παλεύει μαζί του. Ο κοινός υπολογισμός (`computeCompletedUsed`, χρησιμοποιείται και
από τα δύο apps) προσθέτει πλέον αυτό το adjustment πριν κόψει στο sessions_total —
άρα δουλεύει σωστά και προς τις δύο κατευθύνσεις, μόνιμα, όσες φορές κι αν ξανανοίξει
η κάρτα του πελάτη ή όσα ακόμα sessions ολοκληρωθούν μετά.

**Αρχεία:** νέο `sql/019_package_used_adjustment.sql`, `src/sessionsMath.js`
(computeCompletedUsed), `src/TrainerApp.jsx` (handleEditPkg), tests σε
`src/sessionsMath.test.js`

---

## 10. "Adjust Package" δεν έστελνε καμία ειδοποίηση στον πελάτη

**Αιτία:** Όταν ο trainer προσθέτει/αφαιρεί sessions ή επεκτείνει την ημερομηνία
λήξης μέσω "Adjust Package", ο πελάτης δεν μαθαίνει ΠΟΤΕ γι' αυτό — καμία
ειδοποίηση, ούτε στο κουδουνάκι ούτε push. Ακριβώς το "notice που δίνουμε σε
πελάτη όταν μπαίνουν πακέτα" που ζητήθηκε να ελέγξουμε.

**Διόρθωση:** Προστέθηκε notification (νέος τύπος `package_updated`, απλό
toast/κουδουνάκι — ΟΧΙ το μεγάλο modal, για να μη γίνει άλλη μια πηγή υπερβολικών
popup όπως το πρόβλημα με τα low-sessions/slot-requests που διορθώσαμε νωρίτερα)
όποτε πραγματικά αλλάζει κάτι: sessions προστέθηκαν/αφαιρέθηκαν, νέα ημερομηνία
λήξης, ή διόρθωση του "used".

**Αρχεία:** `src/TrainerApp.jsx` (handleEditPkg), `src/ClientApp.jsx` (typeIcon)
